apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: reverse-words
  name: reverse-words
spec:
  template:
    spec:
      containers:
      - env:
        - name: RELEASE
          value: Stage Release
        - $patch: replace
        image: image-registry.openshift-image-registry.svc:5000/reversewords-ci/reversewords:d7fe439bd1f4bd4296a6d8c5d700826716a3df6a
        name: reverse-words
