apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: reverse-words
  name: reverse-words
spec:
  template:
    spec:
      containers:
      - env:
        - name: RELEASE
          value: Production release
        - $patch: replace
        image: default-route-openshift-image-registry.apps.am-week-test.okd.endava-test-domain.be/reversewords-ci/reversewords:latest
        name: reverse-words
